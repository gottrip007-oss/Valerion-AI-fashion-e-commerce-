# Razorpay test/sandbox publishable key (starts with rzp_test_)
# Get one free at https://dashboard.razorpay.com/app/keys after enabling test mode.
# If left blank, checkout falls back to a demo "mark as paid" flow so the
# order flow can still be reviewed end-to-end.
VITE_RAZORPAY_KEY_ID=
